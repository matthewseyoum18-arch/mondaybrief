"""Streamlit dashboard surface — owner-facing CSV upload + past briefs.

Two pages only (per v1 plan): `Upload Customers` and `Past Briefs`. No admin
portal, no scoring sliders — those are deferred to v2.

Entry point: ``streamlit run -m mondaybrief.ui.streamlit_app`` or via the
console script declared in ``pyproject.toml``.
"""
